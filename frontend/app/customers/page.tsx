// TODO(intern): simple list + create form, doesn't need /jobs-level polish.
// See docs/assignment-brief.md Section 8.
export default function CustomersPage() {
  return (
    <div className="flex-1 bg-paper p-6">
      <h1 className="font-heading text-xl font-semibold text-ink">Customers</h1>
      <p className="mt-2 text-sm text-ink-muted">TODO: build the customers list + create form here.</p>
    </div>
  );
}
